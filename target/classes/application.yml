spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/chatgpt

minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucket: chatgpt

server:
  port: 8080
