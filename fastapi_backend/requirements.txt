fastapi[standard]==0.116.1
uvicorn[standard]==0.35.0
motor==3.7.1
pymongo==4.14.1
minio==7.2.18
httpx==0.28.1
python-multipart==0.0.20
pydantic-settings==2.10.1

# ── MCP server ──────────────────────────────────────────────────────────────
# fastmcp 2.x declares a broad starlette dependency, but fastapi 0.116.x
# requires starlette<0.48.0.  Pin starlette explicitly to keep pip happy.
starlette>=0.40.0,<0.48.0
fastmcp>=2.0,<3.0

# ── Testing ─────────────────────────────────────────────────────────────────
pytest-asyncio>=0.24.0,<1.0.0
