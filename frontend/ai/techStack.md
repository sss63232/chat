# Tech Stack

## 已確認

- Next.js 15.5.x
- React 18.2.x
- TypeScript 5.5.x
- Next.js App Router
- client-side React hooks + streaming fetch

## 目前可見依賴

- `react`, `react-dom`
- `next`
- `typescript`
- `@types/react`, `@types/react-dom`, `@types/node`

## 潛在預期

- Tailwind CSS（目前尚未安裝）
- Redux / Redux Toolkit（專案規則要求，但目前尚未建立）

## 推薦補齊

- 若要完全遵循專案規則，應新增 `redux` 與 `@reduxjs/toolkit`，並建立 `store/`。
- 若確定使用 Tailwind，應新增 `tailwindcss`, `postcss`, `autoprefixer` 及相關設定檔。